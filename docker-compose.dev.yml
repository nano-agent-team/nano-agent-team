version: '3.9'

# Development compose — builds image locally, mounts ./data for persistence
# Usage: docker compose -f docker-compose.dev.yml up --build

services:
  nano-agent-team:
    build:
      context: .
      dockerfile: Dockerfile
    image: nano-agent-team:dev
    container_name: nano-agent-team
    restart: unless-stopped
    stop_grace_period: 60s
    # host networking is required for Claude Code OAuth login:
    # claude auth login opens a local callback server on a random port,
    # and the Anthropic redirect goes to localhost — must be the same namespace.
    network_mode: host
    volumes:
      # Persistent data: config, SQLite, sessions, vault, workspaces
      - ./data:/data
      # Docker socket — needed to spawn agent containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Claude Code credentials — shared with host so OAuth tokens persist
      - ~/.claude:/root/.claude
    environment:
      DATA_DIR: /data
      HOST_DATA_DIR: ${PWD}/data
      LOG_LEVEL: info
      # Embedded NATS (default — no NATS_URL needed)
      # To use external NATS: set NATS_URL=nats://nats:4222 and NATS_EMBEDDED=false
      # NATS_URL: nats://nats:4222
      # NATS_EMBEDDED: "false"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
